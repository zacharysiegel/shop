# This file lists the full property paths for all secrets used with Authelia.
# The setup script reads this file and generates the expected environment variables and secret files.
# All properties listed here should have an analogous encrypted secret managed by the `crypt` application.
---
- identity_validation.reset_password.jwt_secret
- session.secret
- storage.encryption_key
- storage.postgres.password
...
